#include "ConfigReader.h"
#include <stdio.h>

int main() {

    // BacaBanyakBangunan();
    // printf("%d\n",BanyakBangunan(BacaConfig));
    int i;
    for (i=1;i<10;i++) {
        STARTCONFIG(i);
        printf("%c\n",CC);
    }

    return 0;
}